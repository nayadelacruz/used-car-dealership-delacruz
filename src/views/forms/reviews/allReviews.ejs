<%- include('../../partials/header') %>

<main class="userReviews">

    <h1><%= title %></h1>

    <% if (reviews.length === 0) { %>

        <p>There are currently no reviews.</p>

    <% } else { %>

        <table class="reviewsTable">

            <thead>
                <tr>
                    <th>User</th>
                    <th>Vehicle</th>
                    <th>Review</th>
                    <th>Date</th>
                    <th>Action</th>
                </tr>
            </thead>

            <tbody>

                <% reviews.forEach(review => { %>

                    <tr>

                        <td>
                            <%= review.user_name %>
                        </td>

                        <td>
                            <%= review.make %> <%= review.model %>
                        </td>

                        <td>
                            <%= review.comment %>
                        </td>

                        <td>
                            <%= new Date(review.created_at).toLocaleDateString() %>
                        </td>

                        <td>

                            <form
                                action="/reviews/admin-delete/<%= review.review_id %>"
                                method="POST"
                                style="display:inline;">

                                <button
                                    type="submit"
                                    class="bottonDelete"
                                    onclick="return confirm('Are you sure you want to delete this review?')">

                                    Delete

                                </button>

                            </form>

                        </td>

                    </tr>

                <% }); %>

            </tbody>

        </table>

    <% } %>

</main>

<%- include('../../partials/footer') %>